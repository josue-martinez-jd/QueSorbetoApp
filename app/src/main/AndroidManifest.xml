<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quesorbeto.quesorbetoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".Client.ClientListActivity">
        </activity>
        <activity android:name=".Client.ClientAddActivity">
        </activity>
        <activity android:name=".Product.ProductAddActivity">
        </activity>
        <activity android:name=".Product.ProductListActivity">
        </activity>
        <activity android:name=".Invoice.InvoiceAddActivity">
        </activity>
        <activity android:name=".Invoice.InvoiceListActivity">
        </activity>
        <activity android:name=".Invoice.InvoiceClientListSelectionActivity">
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>